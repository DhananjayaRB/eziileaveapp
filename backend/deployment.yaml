apiVersion: v1
kind: Service
metadata:
  namespace: qa  # Replace {{Environment}} with actual value
  name: eziileave-api
  labels:
    app: eziileave-api
spec:
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
  selector:
    app: eziileave-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: eziileave-api
  name: eziileave-api
  namespace: qa  # Replace {{Environment}} with actual value
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eziileave-api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      annotations:
      labels:
        app: eziileave-api
      name: eziileave-api
    spec:
      nodeSelector:
        env: nonprod
      dnsConfig:
        searches:
          - svc.cluster.local
        options:
          - name: ndots
            value: '2'
      containers:
      - name: eziileave-ui
        image: resolvepay.azurecr.io/qa/eziileave-api:{{BUILDTAG}}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4000
          protocol: TCP
        livenessProbe:
          tcpSocket:
            port: 4000
          initialDelaySeconds: 60
          periodSeconds: 60
          successThreshold: 1
          timeoutSeconds: 5
        readinessProbe:
          tcpSocket:
            port: 4000
          initialDelaySeconds: 60
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 5